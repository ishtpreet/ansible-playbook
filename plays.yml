---
  - hosts: ansible_azure
    become: true
    tasks:
    - name: Installing Multiple Packages
      apt: name={{item}} state=present update_cache=yes
      with_items:
      - python-pip
      - python3-pip
      - python-virtualenv